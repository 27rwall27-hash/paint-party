services:
  - type: web
    name: paint-party
    runtime: node
    plan: free
    buildCommand: npm install -g npm@11 && npm ci && npm run build
    startCommand: npm run start
    envVars:
      - key: NODE_VERSION
        value: 20.18.0
